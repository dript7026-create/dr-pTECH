[project]
name = "driptriumvirate"
version = "0.1.0"
description = "drIpTriumvirate — feel-first ad engine with Matrix task management and dubstep"
requires-python = ">=3.11"
dependencies = ["Pillow>=10.0"]

[project.scripts]
driptriumvirate = "triumvirate.server:main"
